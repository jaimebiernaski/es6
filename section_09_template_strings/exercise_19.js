/*
 * Reactor the functions to use template strings
 */

function doubleMessage(number) {
  return "Your number doubled is " + (2 * number);
}

function doubleMessage(number) {
  return `Your number doubled is ${2 * number}`;
}
